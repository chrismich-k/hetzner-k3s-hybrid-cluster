---
apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  namespace: traefik
spec:
  ports:
    - name: http
      port: 4180
      targetPort: 4180
      protocol: TCP
  selector:
    # this selector must exactly match the labels in your deployment.yaml!
    app: oauth2-proxy
  type: ClusterIP
